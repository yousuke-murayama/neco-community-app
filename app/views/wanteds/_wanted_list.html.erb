<figure class='wanted-image'>
    <% wanteds.each do |wanted| %>
      <%= link_to wanted_path(wanted) do %>
        <div class="count_box">
          <%= image_tag wanted.image.url, size: '250x250' if wanted.image? %>
          <div class="human_icon">
            <i class="far fa-user"></i><%= wanted.cooperations.count %>
          </div>
        </div>
        <figcaption>
          <p><%= wanted.address %></p>
          <p><%= wanted.date %></p>
        </figcaption>
      <% end %>
    <% end %>
 </figure>

